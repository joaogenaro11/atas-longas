# Alternativa ao uv (pip). O caminho recomendado é 'uv sync' (ver install.sh).
flask>=3.0
psutil>=5.9

# Backend multiplataforma (Intel Mac / Linux / Windows / Docker, CPU)
faster-whisper>=1.1.0

# Backend otimizado para Apple Silicon (instale só no Mac, nativamente):
#   pip install "mlx-whisper>=0.4.0"
